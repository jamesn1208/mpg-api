[project]
name = "fuel-api"
version = "0.1.0"
description = "API service to feed fuel data to the frontend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "requests>=2.34.2",
    "toml>=0.10.2",
    "uvicorn>=0.47.0",
]
